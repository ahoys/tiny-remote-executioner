{
  "name": "tiny-remote-executioner",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production bun src/index.ts",
    "install": "NODE_ENV=production pm2 start bun --name trshell -- run start",
    "watch": "NODE_ENV=development bun --watch src/index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}